[pytest]
# Put both the top-level src/ (datacite, genie, snowflake_utils) and dags/ on the
# import path. Example: `dags` on the path lets tests do
# `from dags import <dag_module>`.
pythonpath = src dags
testpaths = tests
