[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "datacite"
version = "0.1.0"
dependencies = [
    "requests ~=2.31",
]

[tool.setuptools]
packages = ["datacite"]

[tool.setuptools.package-dir]
datacite = "."